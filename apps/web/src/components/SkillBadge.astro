---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<span class="inline-block py-1 px-2 rounded-full text-[11px] font-medium uppercase tracking-[0.06em] text-foreground bg-card border border-border">
  {name}
</span>
