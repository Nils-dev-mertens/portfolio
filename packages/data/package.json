{
  "name": "@portfolio/data",
  "version": "0.0.1",
  "type": "module",
  "main": "./index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "dev": "bun src/run-scheduler.ts",
    "build": "echo 'data package — no build step'",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "DB_PATH=../../apps/web/portfolio.db bun src/seed.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "bun-types": "^1.3.11",
    "drizzle-kit": "^0.31.10"
  }
}
