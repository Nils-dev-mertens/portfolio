{
  "name": "portfolio",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "seed": "bun packages/data/src/seed.ts",
    "start": "bun apps/web/dist/server/entry.mjs"
  },
  "packageManager": "bun@1.2.5",
  "devDependencies": {
    "turbo": "^2.9.3"
  }
}
